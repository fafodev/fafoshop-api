package fafoshop.pos.category.dto;

import fafoshop.common.dto.request.AbstractRequest;

/** Không cần tham số — lấy toàn bộ category_type đang tồn tại (chưa xoá mềm). */
public class CategoryTypeListRequest extends AbstractRequest {
}
