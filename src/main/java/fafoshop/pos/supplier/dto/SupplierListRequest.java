package fafoshop.pos.supplier.dto;

import fafoshop.common.dto.request.AbstractRequest;

/** Không cần tham số — lấy toàn bộ nhà cung cấp còn hiệu lực, phục vụ dropdown ở form Product Master. */
public class SupplierListRequest extends AbstractRequest {
}
